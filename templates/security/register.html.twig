{% extends 'base.html.twig' %}

{% block title %}Регистрация{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-sm-12">
                {{ form_start(registrationForm, { attr: { class: 'form-signin user-register' } }) }}
                <h1 class="h3 mb-3 font-weight-normal">Регистрация</h1>
                {{ form_end(registrationForm) }}
            </div>
        </div>
    </div>
{% endblock %}
